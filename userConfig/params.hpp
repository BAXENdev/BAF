
// Mission Parameters Go Here

// class name1
// {
//     title = "Item 1";
//     texts[] = { "One", "Two", "Three" };
//     values[] = { 1, 2, 3 };
//     default = 1;
//     // function = "FUNC_fnc_FUNCTIONNAME"; // A function to run at mission start
//     // isGlobal = 1; // If 1, the script will be run on every client. If 0 or undefined, it only runs on the server
// };

// Access a mission parameter with
// _param = ["name1", -1] call BIS_fnc_getParamValue;


