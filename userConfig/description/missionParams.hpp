
// Mission Parameters Go Here
// can be picked on mission slotting screen

// class name1
// {
//     title = "Item 1";
//     texts[] = { "One", "Two", "Three" };
//     values[] = { 1, 2, 3 };
//     default = 1;
//     // function = "TAG_fnc_FUNCTION"; // A function to run at mission start
//     // isGlobal = 1; // If 1, the script will be run on every client. If 0 or undefined, it only runs on the server
// };

// Access a mission parameter with
// _param = ["name1",nil] call BIS_fnc_getParamValue;


