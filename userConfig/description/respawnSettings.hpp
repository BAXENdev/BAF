
#include "..\..\macros\utilityMacros.hpp"

// West / blufor
CFG_RESPAWN_WEST_TICKETS = -1;          // Tickets shared by side | -1 = infinite
CFG_RESPAWN_WEST_TICKETS_PERSONAL = -1; // Tickets unique to individual (used before side) | -1 = infinite
CFG_RESPAWN_WEST_DELAY = 0;             // Delay in respawn. Sets wave delay if wave is active.
CFG_RESPAWN_WEST_SQUAD = 0;             // Add squad member spawn points | 0 = false | 1 = true
CFG_RESPAWN_WEST_WAVE = 0;              // Do wave respawn | 0 = false | 1 = true
CFG_RESPAWN_WEST_WAVE_DURATION = 0;     // Duration of respawn wave
CFG_RESPAWN_WEST_WAVE_CALL = 0;         // Allow squad leaders to call respawn wave

// East / opfor
CFG_RESPAWN_EAST_TICKETS = -1;          // Tickets shared by side | -1 = infinite
CFG_RESPAWN_EAST_TICKETS_PERSONAL = -1; // Tickets unique to individual (used before side) | -1 = infinite
CFG_RESPAWN_EAST_DELAY = 0;             // Delay in respawn. Sets wave delay if wave is active.
CFG_RESPAWN_EAST_SQUAD = 0;             // Add squad member spawn points | 0 = false | 1 = true
CFG_RESPAWN_EAST_WAVE = 0;              // Do wave respawn | 0 = false | 1 = true
CFG_RESPAWN_EAST_WAVE_DURATION = 0;     // Duration of respawn wave
CFG_RESPAWN_EAST_WAVE_CALL = 0;         // Allow squad leaders to call respawn wave

// Independent / guerilla
CFG_RESPAWN_GUER_TICKETS = -1;          // Tickets shared by side | -1 = infinite
CFG_RESPAWN_GUER_TICKETS_PERSONAL = -1; // Tickets unique to individual (used before side) | -1 = infinite
CFG_RESPAWN_GUER_DELAY = 0;             // Delay in respawn. Sets wave delay if wave is active.
CFG_RESPAWN_GUER_SQUAD = 0;             // Add squad member spawn points | 0 = false | 1 = true
CFG_RESPAWN_GUER_WAVE = 0;              // Do wave respawn | 0 = false | 1 = true
CFG_RESPAWN_GUER_WAVE_DURATION = 0;     // Duration of respawn wave
CFG_RESPAWN_GUER_WAVE_CALL = 0;         // Allow squad leaders to call respawn wave

// Civilian / acceptable casualties
CFG_RESPAWN_CIV_TICKETS = -1;          // Tickets shared by side | -1 = infinite
CFG_RESPAWN_CIV_TICKETS_PERSONAL = -1; // Tickets unique to individual (used before side) | -1 = infinite
CFG_RESPAWN_CIV_DELAY = 0;             // Delay in respawn. Sets wave delay if wave is active.
CFG_RESPAWN_CIV_SQUAD = 0;             // Add squad member spawn points | 0 = false | 1 = true
CFG_RESPAWN_CIV_WAVE = 0;              // Do wave respawn | 0 = false | 1 = true
CFG_RESPAWN_CIV_WAVE_DURATION = 0;     // Duration of respawn wave
CFG_RESPAWN_CIV_WAVE_CALL = 0;         // Allow squad leaders to call respawn wave
