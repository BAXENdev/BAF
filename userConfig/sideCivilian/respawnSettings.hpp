
civilian_respawnTickets          = -1;       // Tickets shared by side | -1 = infinite
civilian_respawnTicketsPersonal  = -1;       // Tickets unique to individual (used before side) | -1 = infinite
civilian_respawnDelay            = 0;        // Delay in respawn. Sets wave delay if wave is active.
civilian_respawnOnSquad          = 0;        // Add squad member spawn points | 0 = false | 1 = true
civilian_respawnOnGroups         = 0;        // Add squad member spawn points | 0 = false | 1 = true
